{
  "name": "pcap-pin-renderer",
  "version": "1.0.1",
  "description": "Puppeteer microservice for PCAP pin image rendering",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "npx puppeteer browsers install chrome",
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.0.0"
  }
}
